import {blockContentType} from './blockContentType'
import {postType} from './postType'
import {productType} from './productType'


export const schema = {
  types: [blockContentType, postType, productType],
}
